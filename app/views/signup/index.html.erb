<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>

<ul class="signup">
  <% @signup.each do |signup| %>
    <li>
       <%= link_to signup.first_name, signup %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>